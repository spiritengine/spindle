[project]
name = "spindle"
version = "0.1.0"
description = "MCP server for Claude Code to Claude Code delegation"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.0.0",
]

[project.scripts]
spindle = "spindle:mcp.run"
